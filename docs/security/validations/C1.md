# C1 — CVE-2026-23869 independent verification

- Candidate: `C1` / CVE-2026-23869
- Domain: Framework / RSC decoder
- Verifier: security-verifier (did not author the test)
- Revision: `7dca4f4ab72ede551f592afb58f69616cb80b284`
- Date: 2026-09-07
- **Verdict: `validated`**

## Command (unchanged)

```bash
python3 /tmp/security-review/validate_cve_2026_23869.py
```

Working directory: `/workspace`

The script was not edited. Product code was not edited.

## Outcome

| Field | Value |
| --- | --- |
| Exit code | `1` |
| Stdout | empty |
| Stderr | `AssertionError: CVE-2026-23869: next@15.1.4 is in affected range >=13.0.0 <15.5.15; expected >=15.5.15` |
| Failure class | claimed assertion (version pin in affected range) |

Full capture: `docs/security/validations/C1/verifier-rerun.log`

## Independent attack-chain reconstruction

Not taken from the authoring agent's summary.

1. **Pin.** `package.json` dependencies pin `"next": "15.1.4"` (line 38) and `"react": "19.0.0"` (line 40). Installed `node_modules/next/package.json` version is also `15.1.4`.
2. **Public decoder entry.** App Router handles `Next-Action` POSTs in `node_modules/next/dist/server/app-render/action-handler.js` before application auth.
3. **Decode before lookup (multipart fetch, Node runtime).** For `isMultipartAction && isFetchAction`, the handler assigns `boundActionArguments = await decodeReplyFromBusboy(...)` at line 462, then later `actionModId = actionModId ?? getActionModIdOrError(...)` at line 540. The script's `str.find` checks for those exact snippets passed: execution reached the version assertion at script line 39.
4. **Claimed property.** Patched floor for this CVE on Next 15.x is `>=15.5.15`. `15.1.4 < 15.5.15`, so the pin is in the stated affected range `>=13.0.0 <15.5.15`.

Challenged edge from the session (no user `"use server"` modules) does not break the chain: decode still runs before action-id lookup.

## Assertion vs received

| Check | Expected | Received | Result |
| --- | --- | --- | --- |
| `decodeReplyFromBusboy` present | found | found (handler line 462) | pass |
| `getActionModIdOrError` (`actionModId ??`) present | found | found (handler line 540) | pass |
| decode offset `<` lookup offset | true | true | pass |
| `parse(next_ver) >= (15, 5, 15)` | next `>=15.5.15` | `next@15.1.4` | **fail — claimed reason** |
| `parse(react_ver) >= (19, 0, 5)` | react `>=19.0.5` | not reached (`react` is `19.0.0`) | not executed |

The independent rerun failed for the claimed reason, not a setup or import error.

## Verdict rationale

Every required edge that this reproduction tests holds, and the unchanged command failed on the version-pin assertion. Status: **validated**.
