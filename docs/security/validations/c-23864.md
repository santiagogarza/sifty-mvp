# C-23864 independent verification

Candidate: `C-23864` (CVE-2026-23864 Next.js / React Server Components pin).

Verifier did not author the test. Product code and the test were not modified.

## Claim reconstructed from the tree (not from the specialist summary)

- `/workspace/package.json` line 38 pins `"next": "15.1.4"`; lines 40–41 pin `"react": "19.0.0"` and `"react-dom": "19.0.0"`.
- Claimed affected Next 15.1 range: `>=15.1.0 <15.1.12` (fixed in `15.1.12`). `15.1.4 < 15.1.12`, so the pin is inside the range.
- `app/(auth)/sign-in/page.tsx` lines 6–17: `export default async function SignInPage` with no `"use client"`.
- `middleware.ts` lines 25–36, 45–48: `PROTECTED_PREFIXES` does not include `/sign-in`; unmatched paths call `NextResponse.next()`.
- Negative control: `app/(app)/inbox/page.tsx` line 1 is `"use client"`.

## Command (unchanged)

```bash
node /tmp/sifty-security/validations/c-23864/assert-patched.mjs
```

## Assertion

The harness asserts `next` is patched (`not` in `>=15.1.0 <15.1.12`). On this revision it is expected to **fail** with exit `1` because `15.1.4 < 15.1.12`. Harness/setup errors exit `2` with `HARNESS ERROR`.

## Independent rerun

| Field | Expected (patched) | Received |
| ----- | ------------------ | -------- |
| exit code | `0` if patched; `1` if vulnerable pin | `1` |
| `next` | outside `>=15.1.0 <15.1.12` | `15.1.4` |
| `nextIsPatched` | `true` | `false` |
| `nextIsVulnerable` | `false` | `true` |
| stderr | patched message | `ASSERTION FAILED: next@15.1.4 is still in CVE-2026-23864 range ... 15.1.4 < 15.1.12.` |
| harness | no `HARNESS ERROR`, not exit `2` | no harness error |

## Why this is the claimed reason

The rerun failed because **`next@15.1.4` is still in `>=15.1.0 <15.1.12`**. Range negative controls, the sign-in Server Component check, the inbox `"use client"` control, and the `/sign-in` unauthenticated-prefix control all passed (otherwise exit would have been `2`). This is not a setup or import error.

This validates the **vulnerable pin** edge. It does not execute a remote RSC/Flight payload.

## Full stdout

```
{
  "candidate": "C-23864",
  "cve": "CVE-2026-23864",
  "next": "15.1.4",
  "react": "19.0.0",
  "nextVulnerableRange": ">=15.1.0 <15.1.12",
  "nextIsPatched": false,
  "nextIsVulnerable": true,
  "reactAffectedRange": "19.0.0–19.0.3",
  "reactIsAffected": true,
  "signInPage": "app/(auth)/sign-in/page.tsx",
  "signInIsAsyncServerComponent": true,
  "signInHasUseClient": false,
  "negativeControlInboxIsClient": true,
  "negativeControlSignInUnauthenticated": true,
  "protectedPrefixes": [
    "/today",
    "/focus",
    "/inbox",
    "/waiting",
    "/someday",
    "/done",
    "/memory",
    "/settings"
  ]
}
```

## Full stderr

```
ASSERTION FAILED: next@15.1.4 is still in CVE-2026-23864 range >=15.1.0 <15.1.12 (expected patched >= 15.1.12 on the 15.1 line, or otherwise outside that range). 15.1.4 < 15.1.12.
```

## Exit code

`1`

## Verdict

`validated`

## Evidence paths

- `/workspace/docs/security/validations/c-23864.md`
- `/workspace/docs/security/validations/c-23864/verifier-stdout.log`
- `/workspace/docs/security/validations/c-23864/verifier-stderr.log`
- `/workspace/docs/security/validations/c-23864/verifier-exit.code`
- `/tmp/sifty-security/validations/c-23864/verifier-stdout.log`
- `/tmp/sifty-security/validations/c-23864/verifier-stderr.log`
- `/tmp/sifty-security/validations/c-23864/verifier-exit.code`
- `/tmp/sifty-security/validations/c-23864/assert-patched.mjs`
- Ledger: `/workspace/docs/security/security-findings.md` and `/tmp/sifty-security/security-findings.md`
